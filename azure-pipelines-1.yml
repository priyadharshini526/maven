trigger:
- main

pool:
  name: my_machine   # ⚠️ IMPORTANT (un agent pool name)

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean test'
    publishJUnitResults: true
    
